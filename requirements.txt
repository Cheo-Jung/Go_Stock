torch>=2.0.0
transformers>=4.30.0
pandas>=2.0.0
numpy>=1.24.0
yfinance>=1.0.0  # Python 3.10+ supports latest yfinance
cryptography>=3.3.2
requests>=2.31.0
streamlit>=1.28.0
plotly>=5.17.0
python-dotenv>=1.0.0  # .env 파일로 API 키 관리 (선택)

# GPU 지원 (선택사항):
# CPU만 사용하는 경우: 위의 torch 설치로 충분합니다
# GPU 사용 시 (CUDA 11.8): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# GPU 사용 시 (CUDA 12.1): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Google Colab 사용 시: GOOGLE_COLAB_SETUP.md 참조
